PyQt_Login_App/
│
├── main.py   # Application entry point.
│   
├── requirements.txt
│
├── .gitignore
│
├── ui/
│   ├── login_ui.py    # Login window UI
│   │   
│   └── register_ui.py    # Register window UI for creating new users.
|
├── services/
│   └── auth_service.py     # Handles login and register logic (validate user, create user).
│       
├── utils/
│   ├── xml_handler.py      # Reads and writes user data to the XML file.
│   │  
│   └── security.py         # Hashes passwords and verifies them using hashlib.
│      
├── storage/
│   └── users.xml       # Stores user credentials 
|
└── styles/
    └── style.qss
        # UI styling file (colors, buttons, input design).



PyQt6                 → UI
xml.etree.ElementTree → XML read/write
hashlib               → encrypt password


Login Flow (How it works)

User enters username + password
        ↓
PyQt6 UI sends data to auth_service
        ↓
auth_service reads users.xml
        ↓
check username & password
        ↓
if match → Login success
else → Invalid credentials



Register (Create New User)

User clicks Register
        ↓
Enter username + password
        ↓
Check if username already exists in XML
        ↓
If not exist → append to XML
        ↓
Save users.xml


CI/CD Pipeline Arch


CI/CD Pipeline (GitHub Actions)
│
├── Trigger (Events)
│   ├── Push (main / dev)
│   ├── Pull Request
│   ├── Schedule (cron)
│   └── Tag (v1.0.0)
│
├── Continuous Integration (CI)
│   │
│   ├── ci.yml (Setup & Cache)
│   │   ├── Checkout Code
│   │   ├── Setup Python
│   │   ├── Cache Dependencies
│   │   └── Install Requirements
│   │
│   ├── lint.yml (Code Quality)
│   │   ├── flake8 (Style Check)
│   │   ├── black (Formatting Check)
│   │   └── pylint (Code Quality Score)
│   │
│   ├── test.yml (Automated Testing)
│   │   ├── Run pytest
│   │   ├── Validate Auth Logic
│   │   └── Generate Coverage Report
│   │
│   └── security.yml (Security Scan)
│       ├── safety (Dependency Vulnerabilities)
│       └── bandit (Code Security Issues)
│
├── Continuous Delivery (CD)
│   │
│   ├── build.yml (Build Application)
│   │   ├── Install Dependencies
│   │   ├── Run PyInstaller
│   │   └── Generate .exe File
│   │
│   └── release.yml (Release Management)
│       ├── Trigger on Tag (v*)
│       ├── Build .exe
│       ├── Create GitHub Release
│       └── Upload Executable
│
├── Artifacts
│   ├── .exe File
│   ├── Test Reports
│   └── Coverage Reports
│
└── Notifications (Optional)
    ├── Email Notification
    └── Scheduled Jobs (cron)


Trigger → Starts the workflow
CI → Checks code quality, testing, and security
CD → Builds and releases application
Artifacts → Output files
Notifications → Alerts and scheduled automation